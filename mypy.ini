[mypy]
python_version = 3.14
strict = True
ignore_missing_imports = True
exclude = saves/|tests/|\.venv/

# Note: mypy does not yet ship wheels for CPython 3.14.
# Run manually when compatible:
#   pip install mypy
#   python -m mypy worldbuildingengine/

[mypy-worldbuildingengine.types]
ignore_errors = True
